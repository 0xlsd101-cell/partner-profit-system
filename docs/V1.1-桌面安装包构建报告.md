# V1.1 桌面安装包构建报告

## 1. 构建结论

- Rust / Cargo：已安装并可用
- MSVC Build Tools：已安装并可用
- `npm.cmd run tauri:build`：通过
- Windows 安装包：已生成
- 桌面应用：已启动验证
- SQLite：已初始化，数据库文件不在源码目录
- Web 版：`npm.cmd run build` 仍通过
- 是否修改收益业务逻辑：否
- 是否修改数据库结构含义：否，仅新增桌面 SQLite 存储实现和 migration
- 是否影响 V1.0.2 GitHub Pages：否，本次未改动发布配置，也未部署

## 2. 工具链版本

| 工具 | 版本 |
| --- | --- |
| cargo | 1.96.1 |
| rustc | 1.96.1 |
| rustup | 1.29.0 |
| MSVC Build Tools | Visual Studio BuildTools 2022 |

## 3. 命令验证结果

| 命令 | 结果 | 备注 |
| --- | --- | --- |
| `npm.cmd run test` | 通过 | 6 个测试文件，97 个用例 |
| `npm.cmd run lint` | 通过 | 无 lint 阻塞 |
| `npm.cmd run build` | 通过 | Web / GitHub Pages 构建保持可用 |
| `npm.cmd run desktop:build` | 通过 | 桌面前端相对路径构建成功 |
| `npm.cmd run tauri:build` | 通过 | 已生成 Windows NSIS 安装包 |

构建期间仍存在 Vite chunk 体积提示，属于既有非阻塞提示。

## 4. 安装包路径

```text
C:\Users\1566\Documents\合伙人月度收益计算与年度分红汇总系统\src-tauri\target\release\bundle\nsis\合伙人收益系统_1.1.0_x64-setup.exe
```

同时生成桌面可执行文件：

```text
C:\Users\1566\Documents\合伙人月度收益计算与年度分红汇总系统\src-tauri\target\release\partner-profit-system.exe
```

## 5. 安装验证

已使用静默方式安装到当前用户测试目录：

```text
C:\Users\1566\AppData\Local\Programs\PartnerProfitSystemV11Test
```

安装结果：

- 安装程序退出码：0
- 已生成应用 exe
- 已生成卸载程序
- 安装后的 exe 可启动

安装后的应用路径：

```text
C:\Users\1566\AppData\Local\Programs\PartnerProfitSystemV11Test\partner-profit-system.exe
```

## 6. SQLite 初始化验证

桌面应用启动后生成 SQLite 文件：

```text
C:\Users\1566\AppData\Roaming\com.partnerprofit.system\partner-profit-system.sqlite
```

只读检查结果：

- `schemaVersion = 1`
- 核心表已创建
- 所有业务表当前记录数为 0
- 数据库文件不在项目源码目录

核心表：

- `members`
- `capital_lots`
- `capital_transactions`
- `monthly_settlements`
- `monthly_allocations`
- `adjustment_records`
- `dividend_payments`
- `annual_dividend_confirmations`
- `operation_logs`
- `profit_calculator_records`
- `app_metadata`

## 7. JSON 导入 / 导出验证

桌面版继续复用 Web 版“数据备份”页面和 `PartnerRepository` 接口：

- 导出完整 JSON 备份入口保留
- 导入 JSON 备份入口保留
- 导入前中文摘要逻辑保留
- locked / adjusted 明细保护逻辑保留
- 危险清除确认逻辑保留

本次自动验证未导入真实 JSON 备份，也未写入真实业务数据。原因是本任务要求不得覆盖正式数据，且当前环境没有独立 GUI 自动化能力执行完整页面导入 / 导出闭环。建议后续在独立桌面验收库中人工导入一份脱敏备份，完成最终业务级恢复演练。

## 8. 数据安全确认

- 未清空 IndexedDB
- 未读取浏览器 IndexedDB
- 未删除 `release-evidence/`
- 未删除 `data/trial/`
- 未改动 `.env`、密钥或配置文件
- 未修改 `src/domain/calculation.ts`
- 未迁移、重算或覆盖 locked / adjusted 历史月份
- 用户数据未被打包进安装包
- 安装包位于 `src-tauri/target/`，该目录已被 `.gitignore` 排除

## 9. 已处理的问题

1. Rust / Cargo 缺失：已安装 Rustup，并设置 `stable-msvc`。
2. MSVC linker 缺失：已安装 Visual Studio BuildTools 2022 C++ 工具链。
3. Tauri Windows 图标缺失：已新增 `src-tauri/icons/icon.ico`。

## 10. 仍需后续处理的问题

- 建议在独立验收库中人工完成 JSON 导入 / 导出业务闭环。
- 如需对外发布安装包，建议增加代码签名。
- 如需最终提交仓库，注意不要提交 `src-tauri/target/` 或真实数据文件。
- 当前未上传安装包到 GitHub，需用户确认后再发布安装包。
