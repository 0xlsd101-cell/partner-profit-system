# V1.1 桌面离线版脱敏数据闭环验收报告

## 1. 验收结论

- 脱敏数据导入：通过，采用脱敏 JSON 备份写入桌面 SQLite 测试库。
- SQLite 持久化：通过，关闭并重新启动桌面应用后数据仍存在。
- JSON 导出：通过，已从桌面 SQLite 生成脱敏完整备份。
- JSON 恢复：通过，使用导出的 JSON 恢复到独立 SQLite 测试库后金额一致。
- CSV / XLSX 导出：通过，已生成中文 CSV 和 XLSX 核对文件，XLSX 可用 ExcelJS 读回。
- 桌面应用启动：通过，安装后的 exe 可启动。
- 命令验证：通过，test / lint / build / desktop:build / tauri:build 均成功。
- 建议版本：建议标记为 V1.1.0-beta；补做一次人工桌面 GUI 文件选择导入 / 页面导出点击验收后，可定版为 V1.1.0 桌面离线版。

说明：当前工具环境无法可靠自动点击 Tauri 桌面窗口和系统文件选择器。本次已完成 SQLite repository 数据闭环、持久化、恢复和导出文件验证，但“通过桌面页面手动点击导入 / 导出”的 GUI 级闭环仍建议人工补验。

## 2. 环境信息

| 项目 | 结果 |
| --- | --- |
| Rust / Cargo | cargo 1.96.1 |
| rustc | 1.96.1 |
| rustup | 1.29.0 |
| 安装包路径 | `src-tauri/target/release/bundle/nsis/合伙人收益系统_1.1.0_x64-setup.exe` |
| 应用安装路径 | `C:\Users\1566\AppData\Local\Programs\PartnerProfitSystemV11Test` |
| SQLite 文件路径 | `C:\Users\1566\AppData\Roaming\com.partnerprofit.system\partner-profit-system.sqlite` |
| schemaVersion | 1 |
| 数据来源 | 脱敏验收备份，姓名仅使用“负责人、合伙人A、合伙人B” |

## 3. 导入验收

| 项目 | 结果 |
| --- | --- |
| 导入文件 | `release-evidence/v1.1/脱敏闭环/input/脱敏验收备份-2026-07.json` |
| 导入方式 | 写入桌面 SQLite 测试库，使用与桌面 repository 一致的 payload 表结构 |
| 导入摘要 | 合伙人 3；资金批次 3；资金流水 3；月度结算 1；月度收益明细 3；分红支付 0；操作日志 1 |
| 导入结果 | 通过 |
| 是否使用真实敏感数据 | 否 |

导入后的数据数量：

| 数据项 | 数量 |
| --- | ---: |
| 合伙人 | 3 |
| 资金批次 | 3 |
| 资金流水 | 3 |
| 月度结算 | 1 |
| 月度收益明细 | 3 |
| 分红支付记录 | 0 |
| 调整记录 | 0 |
| 年度确认单 | 0 |
| 操作日志 | 1 |
| 收益计算器记录 | 0 |

## 4. 金额核对

| 项目 | 基准值 | 桌面版导入后值 | 重启后值 | 恢复后值 | 差异 | 是否通过 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 本月总折算本金 | 554,838.71 | 554,838.71 | 554,838.71 | 554,838.71 | 0.00 | 通过 |
| 对外合伙人应付收益 | 1,774.19 | 1,774.19 | 1,774.19 | 1,774.19 | 0.00 | 通过 |
| 负责人自有资金收益 | 5,000.00 | 5,000.00 | 5,000.00 | 5,000.00 | 0.00 | 通过 |
| 负责人专项收益 | 7,096.77 | 7,096.77 | 7,096.77 | 7,096.77 | 0.00 | 通过 |
| 负责人理论收益 | 12,096.78 | 12,096.78 | 12,096.78 | 12,096.78 | 0.00 | 通过 |
| 负责人实际净收益 | 23,225.81 | 23,225.81 | 23,225.81 | 23,225.81 | 0.00 | 通过 |
| 实际差额 | 11,129.03 | 11,129.03 | 11,129.03 | 11,129.03 | 0.00 | 通过 |
| 尾差调整 | 0.01 | 0.01 | 0.01 | 0.01 | 0.00 | 通过 |
| 尾差归属 | 负责人 | 负责人 | 负责人 | 负责人 | 0.00 | 通过 |

状态核对：

- 结算月份：2026-07
- 状态：locked
- 年度周期：公历自然年度
- SQLite schemaVersion：1

## 5. 持久化验收

| 检查项 | 结果 |
| --- | --- |
| 关闭并重开桌面应用 | 通过 |
| 重新打开后数据是否保留 | 通过 |
| SQLite 文件是否存在 | 通过 |
| SQLite 文件位置是否在应用数据目录 | 通过 |
| schemaVersion 是否正确 | 通过，值为 1 |
| locked 状态是否保留 | 通过 |

桌面应用重启后核对值：

```text
month = 2026-07
status = locked
totalCapital = 554838.71
managerActualNetProfit = 23225.81
allocationCount = 3
```

## 6. 导出验收

导出文件位于：

```text
release-evidence/v1.1/脱敏闭环/export/
```

| 文件 | 类型 | 结果 |
| --- | --- | --- |
| `桌面版导出完整备份-2026-07.json` | JSON | 已生成 |
| `脱敏月度结算明细-2026年7月.csv` | CSV | 已生成，中文表头 |
| `脱敏年度汇总-2026.csv` | CSV | 已生成，中文表头 |
| `脱敏操作日志-2026.csv` | CSV | 已生成，中文表头 |
| `脱敏月度结算与年度汇总-2026年7月.xlsx` | XLSX | 已生成，可读回 |

XLSX 读回结果：

- Sheet：`月度汇总`
- Sheet：`资金批次明细`
- Sheet：`规则说明`
- 首个金额单元格：554838.71

导出检查：

- 文件名中文：通过
- CSV 表头中文：通过
- XLSX Sheet 中文：通过
- 金额保留两位或可数值读取：通过
- 用户可读 CSV / XLSX 未发现英文技术字段：通过
- JSON 仅作为备份文件，不作为普通用户可读报表：符合规则

## 7. 恢复验收

恢复使用文件：

```text
release-evidence/v1.1/脱敏闭环/export/桌面版导出完整备份-2026-07.json
```

恢复目标：

```text
release-evidence/v1.1/脱敏闭环/restore/恢复测试.sqlite
```

恢复结果：

- 数据数量一致：通过
- 关键金额一致：通过
- locked 状态保留：通过
- schemaVersion = 1：通过
- 导入保护未被绕过：未发现异常明细或无法关联明细

## 8. 卸载 / 重装观察

本次未执行卸载 / 重装测试。

原因：

- 卸载测试属于可选项。
- 当前重点是桌面 SQLite repository、脱敏导入、持久化、导出和恢复能力。
- 已记录安装路径和 SQLite 数据路径。

风险说明：

- 当前 SQLite 数据位于用户 Roaming 应用数据目录。
- 安装程序卸载是否保留该目录仍需独立验证。
- 建议 V1.1 正式发布前补一次“卸载后数据是否保留”的人工测试，并在用户说明中明确。

## 9. 命令验证

| 命令 | 结果 |
| --- | --- |
| `npm.cmd run test` | 通过，6 个测试文件，97 个用例 |
| `npm.cmd run lint` | 通过 |
| `npm.cmd run build` | 通过 |
| `npm.cmd run desktop:build` | 通过 |
| `npm.cmd run tauri:build` | 通过 |

非阻塞提示：

- Vite 仍提示部分 chunk 超过 500 kB，属于既有构建体积提示。

## 10. 已知问题

- 未执行真实数据导入，本次仅使用脱敏数据。
- 未完成桌面 GUI 文件选择器级别的自动导入 / 导出点击验收。
- 未签名安装包，Windows 可能显示安全提示。
- 自动更新未实现。
- 启动密码未实现。
- 卸载 / 重装后数据保留策略尚未人工验证。

## 11. 最终建议

当前建议：

- 可作为 V1.1.0-beta 桌面离线版进入人工试用。
- 不建议在未补 GUI 点击验收前直接标记为 V1.1.0 正式桌面版。

建议补充后定版：

1. 用桌面界面手动导入 `脱敏验收备份-2026-07.json`。
2. 在桌面界面点击导出完整 JSON、CSV、XLSX。
3. 验证文件选择器、保存路径和用户提示无异常。
4. 执行一次卸载 / 重装后数据保留观察。
5. 如全部通过，可定版为 V1.1.0 桌面离线版。
