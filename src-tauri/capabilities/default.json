{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认桌面权限，仅允许核心窗口能力和本地 SQLite 访问。",
  "windows": ["main"],
  "permissions": ["core:default", "sql:default", "sql:allow-execute"]
}
